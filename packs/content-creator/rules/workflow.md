### 自媒体内容生产流

1. 先读取 `{{paths.account_profile}}`，确认账号定位、目标用户和表达偏好。
2. 新想法进入 `{{paths.ideas}}`，不要直接跳到成稿。
3. 素材、案例、截图、链接和用户原话进入 `{{paths.materials}}`。
4. 草稿、脚本和单篇内容推进过程进入 `{{paths.drafts}}`。
5. 内容发布后，必须更新 `{{paths.published}}`。
6. 有数据后，必须进入 `{{paths.review}}` 做复盘，并把下一轮选题建议回流到 `{{paths.ideas}}`。
