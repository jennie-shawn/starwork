### 多项目中枢规则

1. 项目注册信息放在 `{{paths.projects}}`。
2. 跨项目联络记录放在 `{{paths.coordination}}`。
3. 候选回写内容先进入 `{{paths.incoming}}`，确认后再合并到共享资源。
4. Hub 自己的本地收发记录放在 `.starwork/handoff/`。
5. 具体项目的进度正文留在各自项目内，中枢只保存定位、索引和共享机制。
