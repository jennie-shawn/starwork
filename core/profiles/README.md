# Profiles

Profiles 把 Core 标准角色映射到具体路径和模板语言。

Profiles 不能改变 Core 语义。

Profile 不只是把 `_system` 翻译成另一种路径。它包括：

- 路径映射
- 模板语言
- 用户可见的能力名称
- CLI 提问语言
- kit 文件夹标签和解释文案

## v0.1 包含

- `zh/`：中文工作区路径和模板
- `en/`：英文工作区路径和模板
