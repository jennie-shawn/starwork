# Writeback Protocol

Candidate updates from satellite projects must enter `.incoming/` first.

Only reviewed, cross-project reusable material may be merged into `identity/`, `lessons/`, `knowledge/`, or `skills/`.
