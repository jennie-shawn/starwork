interface:
  display_name: "StarWork Doctor"
  short_description: "诊断 StarWork 工作台或历史模板，并生成升级蓝图"
  default_prompt: "Use $starworkDoctor to interpret starwork doctor output, explain a safe upgrade path, and generate an upgrade blueprint when requested."

policy:
  allow_implicit_invocation: true
